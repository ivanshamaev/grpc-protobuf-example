syntax = "proto3";

package hello;

// Описание сервиса
service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

// Запрос
message HelloRequest {
  string name = 1;
}

// Ответ
message HelloReply {
  string message = 1;
}